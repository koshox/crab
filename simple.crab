import people for People
fun fn() {
   var p = People.new("kosho", "male")
   p.sayHi()
}

class Family < People {
   var father
   var mother
   var child
   new(f, m, c) {
      father = f
      mother = m
      child  = c
      super("koshox", "male")
   }
}

var f = Family.new("adc", "cda", "dca")
f.sayHi()

fn()
