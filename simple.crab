import people for People
fun fn() {
   var p = People.new("kosho", "male", 20.0)
   p.sayHi()
}

class Family < People {
   var father
   var mother
   var child
   new(f, m, c) {
      father = f
      mother = m
      child  = c
      super("koshox", "male", 25)
   }
}

var f = Family.new("adc", "cda", "dca")
f.sayHi()

fn()
